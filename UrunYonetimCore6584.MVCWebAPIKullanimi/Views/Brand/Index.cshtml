@model Brand

@{
    ViewData["Title"] = Model.Name;
}

<section class="product_section layout_padding">
    <div class="container">
        <div class="heading_container heading_center">
            <h2>
                @Model.Name <span>Marka Ürünler</span>
            </h2>
        </div>
        <partial name="_PartialProduct" model="Model.Products" />
    </div>
</section>